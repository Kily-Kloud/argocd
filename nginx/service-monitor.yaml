apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nginx-monitor
  labels:
    release: monitoring-stack
spec:
  selector:
    matchLabels:
      app: nginx
  endpoints:
  - port: http
    interval: 15s
